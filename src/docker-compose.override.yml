name: rent-acme

services:
  rabbitmq:
    container_name: rabbitmq
    environment:
      - RABBITMQ_DEFAULT_USER=rentacme
      - RABBITMQ_DEFAULT_PASS=Passw0rd!
    ports:
      - 5672:5672
      - 15672:15672

  redis:
    container_name: redis
    command: redis-server --requirepass Passw0rd!
    ports:
      - 6379:6379

  mailhog:
    container_name: mailhog
    ports:
      - 1025:1025
      - 8025:8025

  keycloak-db:
    container_name: keycloak-db
    environment:
      POSTGRES_DB: keycloak_db
      POSTGRES_USER: rent-acme
      POSTGRES_PASSWORD: Passw0rd!
    ports:
      - 5410:5432 

  keycloak:
    container_name: keycloak
    environment:
    - KC_DB=postgres
    - KC_DB_URL=jdbc:postgresql://keycloak-db:5432/keycloak_db
    - KC_DB_USERNAME=rent-acme
    - KC_DB_PASSWORD=Passw0rd!
    - KC_BOOTSTRAP_ADMIN_USERNAME=admin
    - KC_BOOTSTRAP_ADMIN_PASSWORD=Passw0rd!
    - KK_TO_RMQ_URL=rabbitmq
    - KK_TO_RMQ_PORT=5672
    - KK_TO_RMQ_USERNAME=rentacme
    - KK_TO_RMQ_PASSWORD=Passw0rd!
    - KK_TO_RMQ_EXCHANGE=amq.topic
    - KK_TO_RMQ_VHOST=/
    - KC_OTEL=true
    - OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317
    - OTEL_LOGS_EXPORTER=otlp
    - OTEL_EXPORTER_OTLP_PROTOCOL=grpc
    - KEYCLOAK_BACKEND_SECRET=${KEYCLOAK_BACKEND_SECRET}
    ports:
      - 8080:8080
    command:
      - start-dev
      - --import-realm
    volumes:
      - ../configs/keycloak/realm-rent-acme.json:/opt/keycloak/data/import/realm-rent-acme.json
      - ../configs/keycloak/keycloak-to-rabbit-3.0.5.jar:/opt/keycloak/providers/keycloak-to-rabbit-3.0.5.jar
    depends_on:
      - keycloak-db
      - otel-collector
      - mailhog

  prometheus:
    container_name: prometheus
    command:
      - --config.file=/etc/prometheus/prometheus-config.yaml
      - --enable-feature=otlp-write-receiver
    ports:
      - 9090:9090
    restart: unless-stopped
    volumes:
      - ../configs/prometheus/prometheus-config.yaml:/etc/prometheus/prometheus-config.yaml

  grafana:
    container_name: grafana
    ports:
      - 3000:3000
    restart: unless-stopped
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=grafana
    volumes:
      - ../configs/grafana:/etc/grafana/provisioning/datasources
    depends_on:
      - prometheus

  loki:
    container_name: loki
    ports:
      - 3100:3100
    command:
      - -config.file=/etc/loki/loki-config.yaml
    volumes:
      - ../configs/loki/loki-config.yaml:/etc/loki/loki-config.yaml
    depends_on:
      - grafana

  tempo:
    container_name: tempo
    command:
      - -target=all
      - -config.file=/etc/tempo-config.yaml
    volumes:
      - ../configs/tempo/tempo-config.yaml:/etc/tempo-config.yaml
    ports:
      - 3200:3200 # Tempo
      - 4327:4317 # Tempo OTLP GRPC
      - 4328:4318 # Tempo OTLP HTTP
    depends_on:
      - grafana

  otel-collector:
    container_name: otel-collector
    command: 
      - --config=/etc/otel-collector-config.yaml
    volumes:
      - ../configs/otel-collector/otel-collector-config.yaml:/etc/otel-collector-config.yaml
    ports:
      - 1888:1888 # pprof extension
      - 8888:8888 # Prometheus metrics exposed by the Collector
      - 8889:8889 # Prometheus exporter metrics
      - 13133:13133 # health_check extension
      - 4317:4317 # OTLP gRPC receiver
      - 55679:55679 # zpages extension
    depends_on:
      - prometheus
      - grafana
      - loki
      - tempo

  api-gateway:
    container_name: api-gateway
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=8080
      - ASPNETCORE_HTTPS_PORTS=8081
    ports:
      - 5000:8080
      - 5001:8081
    volumes:
      - ${APPDATA}/Microsoft/UserSecrets:/home/app/.microsoft/usersecrets:ro
      - ${APPDATA}/ASP.NET/Https:/home/app/.aspnet/https:ro
    depends_on:
      - user-api
      - inventory-api
      - reservation-api

  user-db:
    container_name: user-db
    environment:
      POSTGRES_DB: user_db
      POSTGRES_USER: rent-acme
      POSTGRES_PASSWORD: Passw0rd!
    ports:
      - 5420:5432

  user-api:
    container_name: user-api
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=8080
      - ASPNETCORE_HTTPS_PORTS=8081
      - KEYCLOAK_BACKEND_SECRET=${KEYCLOAK_BACKEND_SECRET}
    volumes:
      - ${APPDATA}/Microsoft/UserSecrets:/home/app/.microsoft/usersecrets:ro
      - ${APPDATA}/ASP.NET/Https:/home/app/.aspnet/https:ro
    ports:
      - 5211:8080
      - 5212:8081
    depends_on:
      - user-db
      - rabbitmq
      - keycloak

  inventory-db:
    container_name: inventory-db
    environment:
      POSTGRES_DB: inventory_db
      POSTGRES_USER: rent-acme
      POSTGRES_PASSWORD: Passw0rd!
    ports:
      - 5430:5432

  inventory-api:
    container_name: inventory-api
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=8080
      - ASPNETCORE_HTTPS_PORTS=8081
      - KEYCLOAK_BACKEND_SECRET=${KEYCLOAK_BACKEND_SECRET}
    ports:
      - 5221:8080
      - 5222:8081
    volumes:
      - ${APPDATA}/Microsoft/UserSecrets:/home/app/.microsoft/usersecrets:ro
      - ${APPDATA}/ASP.NET/Https:/home/app/.aspnet/https:ro
    depends_on:
      - inventory-db
      - rabbitmq

  reservation-db:
    container_name: reservation-db
    environment:
      POSTGRES_DB: reservation_db
      POSTGRES_USER: rent-acme
      POSTGRES_PASSWORD: Passw0rd!
    ports:
      - 5440:5432

  reservation-api:
    container_name: reservation-api
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=8080 
      - ASPNETCORE_HTTPS_PORTS=8081
      - KEYCLOAK_BACKEND_SECRET=${KEYCLOAK_BACKEND_SECRET}
    volumes:
      - ${APPDATA}/Microsoft/UserSecrets:/home/app/.microsoft/usersecrets:ro
      - ${APPDATA}/ASP.NET/Https:/home/app/.aspnet/https:ro
    ports:
      - 5231:8080
      - 5332:8081
    depends_on:
      - reservation-db
      - rabbitmq

  notification-api:
    container_name: notification-api
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=8080
      - ASPNETCORE_HTTPS_PORTS=8081
      - KEYCLOAK_BACKEND_SECRET=${KEYCLOAK_BACKEND_SECRET}
    ports:
      - 5241:8080
      - 5242:8081
    volumes:
      - ${APPDATA}/Microsoft/UserSecrets:/home/app/.microsoft/usersecrets:ro
      - ${APPDATA}/ASP.NET/Https:/home/app/.aspnet/https:ro
    depends_on:
      - rabbitmq 

  payment-api:
    container_name: payment-api
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=8080
      - ASPNETCORE_HTTPS_PORTS=8081
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
      - STRIPE_PUBLISHABLE_KEY=${STRIPE_PUBLISHABLE_KEY}
      - STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}
      - KEYCLOAK_BACKEND_SECRET=${KEYCLOAK_BACKEND_SECRET}
    ports:
      - 5251:8080
      - 5252:8081
    volumes:
      - ${APPDATA}/Microsoft/UserSecrets:/home/app/.microsoft/usersecrets:ro
      - ${APPDATA}/ASP.NET/Https:/home/app/.aspnet/https:ro
    depends_on:
      - rabbitmq

  saga-orchestration-statemachine-db:
    container_name: saga-orchestration-statemachine-db
    environment:
      POSTGRES_DB: saga_machine_db
      POSTGRES_USER: rent-acme
      POSTGRES_PASSWORD: Passw0rd!
    ports:
      - 5460:5432

  saga-orchestration-statemachine:
    container_name: saga-orchestration-statemachine
    ports:
      - 5271:8080
    environment:
      - DOTNET_ENVIRONMENT=Development
    depends_on:
      - saga-orchestration-statemachine-db
      - rabbitmq