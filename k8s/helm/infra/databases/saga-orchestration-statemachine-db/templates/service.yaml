apiVersion: v1
kind: Service
metadata:
  name: {{ include "saga-orchestration-statemachine-db.fullname" . }}
  labels:
    {{- include "saga-orchestration-statemachine-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "saga-orchestration-statemachine-db.selectorLabels" . | nindent 4 }}
