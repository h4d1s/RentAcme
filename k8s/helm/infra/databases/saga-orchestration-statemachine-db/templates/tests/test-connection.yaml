apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "saga-orchestration-statemachine-db.fullname" . }}-test-connection"
  labels:
    {{- include "saga-orchestration-statemachine-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "saga-orchestration-statemachine-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
